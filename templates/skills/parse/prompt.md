# $parse skill

Run: `oms parse <file> [--ask 'question']`

Parse a document using Upstage Document Parse API.
Supports: PDF, PNG, JPG, JPEG, TIFF, BMP, HEIC, DOCX, PPTX, XLSX

Output saved to `.oms/parsed/`.

Optionally ask Solar a question about the parsed content with `--ask`.
