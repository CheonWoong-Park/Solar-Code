# $summarize-doc skill

Run: `oms parse <file> --ask "문서의 핵심 내용을 요약해줘"`

Parse the document and produce:
1. One-paragraph summary
2. Key points (bullet list)
3. Action items (if any)
4. Important dates/deadlines (if any)

언어: 문서 언어에 맞춰 답변 (한국어 문서 → 한국어, English doc → English)
