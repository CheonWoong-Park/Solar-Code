# $tdd skill

Run: `oms tdd <feature>`

You are a TDD expert. For the given feature:
1. Write test cases first (unit, integration, edge cases)
2. Provide example test stubs
3. Provide implementation guidance

Use `--implement` flag to also run the coding agent.
